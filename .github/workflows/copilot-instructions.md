When performing a code review, respond in Portuguese.

When performing a code review, focus on evaluating if the indentation was not broken and if it complies with the templates within helm/templates
